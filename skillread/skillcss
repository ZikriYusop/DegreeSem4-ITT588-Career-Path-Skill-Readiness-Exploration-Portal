/* Placeholder styling */
input::placeholder {
    font-size: 0.8rem;
    opacity: 0.7;
}

/* Star Rating Colors */
.star-inactive { color: #dee2e6 !important; transition: color 0.2s; }
.star-active { color: #ffc107 !important; transition: color 0.2s; }

/* Fixed CSS Syntax from your screenshots */
.form-control:focus {
    border-color: #6366f1;
    box-shadow: 0 0 0 2px rgba(99, 102, 241, 0.5); /* Fixed 'ring' error */
}

/* Strict 1-Page Print Styling */
@media print {
    .no-print { display: none !important; }
    
    @page {
        size: A4;
        margin: 0; /* Remove margins to prevent 2nd page overflow */
    }
    
    body {
        background: white !important;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact; /* Fixed cross-browser error */
    }

    .print-container {
        width: 100% !important;
        max-width: 100% !important;
        margin: 0 !important;
        padding: 10mm !important;
    }

    .print-card {
        border: none !important;
        box-shadow: none !important;
        /* Forced scale to ensure everything fits on 1 page */
        transform: scale(0.95);
        transform-origin: top center;
    }
}

/* Pill indicators for Summary Page */
.star-pill { height: 8px; width: 25px; border-radius: 4px; }
.bg-light-dark { background-color: #e9ecef; }
.fw-black { font-weight: 900; }
